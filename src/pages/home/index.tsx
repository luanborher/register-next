import Header from '@/components/Header/Header';

const IndexPage = () => (
  <main className="flex flex-col gap-2">
    <Header title="Olá, Administrador!" subtitle="Painel de Controle" profile />
  </main>
);

export default IndexPage;
